buildscript {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

plugins {
    id("com.netflix.dgs.codegen").version("6.2.2").apply(false)
}

subprojects {
    repositories {
        mavenCentral()
    }
}
